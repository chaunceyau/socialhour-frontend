import * as React from 'react';
import { Card } from 'semantic-ui-react';

export interface IEventInformationProps {
}

export default class EventInformation extends React.Component<IEventInformationProps> {
    render() {
        return (
            <Card>

            </Card>
        );
    }
}
